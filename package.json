{
  "name": "app-template",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts",
    "generate": "bunx drizzle-kit generate",
    "migrate": "bunx drizzle-kit migrate",
    "init-db": "bun run server/scripts/init-db.ts",
    "setup": "bun run migrate && bun run init-db",
    "studio": "bunx drizzle-kit studio"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.8",
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "@types/bcryptjs": "^3.0.0",
    "bcryptjs": "^3.0.2",
    "better-auth": "^1.3.12",
    "drizzle-orm": "^0.33.0",
    "hono": "^4.6.2",
    "postgres": "^3.4.4",
    "zod": "^3.23.8"
  }
}